import React, { FC } from 'react'
import { AccordionContainer } from './styled'
import { AccordionProps } from './types'

export const NewAccordion: FC<AccordionProps> = () => {
  return <AccordionContainer>d</AccordionContainer>
}
